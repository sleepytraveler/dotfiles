[alias]
	st = fuzzy status
	lg = fuzzy log
	br = branch
	co = checkout
	cm = commit
	sh = show
	purr = pull --rebase
	dsync = !git reset --hard $1 && git pull --rebase
	restore = "!git config remote.origin.fetch \"+refs/heads/*:refs/remotes/origin/*\" && git fetch origin"
	last = log -1 HEAD
